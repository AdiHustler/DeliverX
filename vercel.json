{
  "buildCommand": "cd client && npm install --legacy-peer-deps --silent && GENERATE_SOURCEMAP=false CI=false npm run build",
  "outputDirectory": "client/build",
  "installCommand": "npm install --silent",
  "framework": null
}