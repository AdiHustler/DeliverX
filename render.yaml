services:
  - type: web
    name: deliverx-ai-route
    env: static
    staticPublishPath: ./
    buildCommand: echo "Static files ready"
    routes:
      - type: rewrite
        source: /*
        destination: /index.html